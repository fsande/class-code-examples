{
  "compilerOptions": {
    "target": "es2022",
    "module": "commonjs",
    "resolveJsonModule": true,                /* Allows importing modules with a ‘.json’ extension */
    "allowJs": true,
    "rootDir": "./",
    "outDir": "./dist",
    "declaration": true,
    "alwaysStrict": true,                     /* Parse in strict mode and emit "use strict" for each source file. */
    "strict": true,                           /* Enable all strict type-checking options. */
    "noImplicitAny": true,                    /* will issue an error whenever it would have inferred any:*/ 
    "noFallthroughCasesInSwitch": true,      /* Report errors for fallthrough cases in switch statement. */
    "noImplicitReturns": true,                /* Report error when not all code paths in function return a value. */
    "forceConsistentCasingInFileNames": true  /* Force all file names to have the same case style. */
  },
  "files": ["arrays-mutability.ts", "callback.ts", "double-array-values.ts", "for-loops.ts", "slice-example.ts", "slice-to-copy.ts", "splice-example.ts", "read-json-file.ts"],
  "exclude": [
        "node_modules",
        "build",
        "lib",
        "test",
        "public"
  ]
}
